<?xml version="1.0" encoding="utf-8"?>
<shape android:shape="oval"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <size
        android:width="@dimen/clickableRoundSize"
        android:height="@dimen/clickableRoundSize" />

    <solid android:color="@color/in" />
    <stroke
        android:color="@color/out"
        android:width="3dp" />
</shape>